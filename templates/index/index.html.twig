{% extends 'base.html.twig' %}

{% block body %}

    {% if app.user %}
    <div id="home" data-user="{{ user.id }}">

    </div>
    {% else %}
    <div id="home">
    </div>
    {% endif %}

{% endblock %}